default:
    description: 'Default build'
    tasks:
        - 'concurrent:first'
        - 'concurrent:second'
        - 'concurrent:third'
        - 'concurrent:fourth'
        - 'concurrent:fifth'
deploy-staging:
    description: 'Deploy - Staging'
    tasks:
        - 'aws_s3:staging'
deploy-production:
    description: 'Deploy - Production'
    tasks:
        - 'concurrent:first'
        - 'concurrent:second'
        - 'concurrent:third'
        - 'concurrent:fourth'
        - 'concurrent:fifth'
        - 'aws_s3:production'
