angular.module('hotsSnapshot', [])
.run(
    ['$rootScope', '$window',
        function ($rootScope, $window) {
            
            console.log('sup from the snapshot k');
            
        }
    ]
)
;